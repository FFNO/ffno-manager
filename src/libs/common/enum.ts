export enum ContactType {
  TENANT,
  SERVICE_WORKER,
}

export enum RequestCategory {
  UNIT_LEASE,
}

export enum InvoiceCategory {
  UNIT_CHARGE,
}

export enum UnitStatus {
  GOOD,
  MAINTAINING,
  BAD,
}
